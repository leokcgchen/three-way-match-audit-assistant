# 剩余能力边界与已落地最小实现（2026-08）

相对交付包「全量完善」，下列项已做**能做的最小落地**；明确不做的仍不宣称完成。

| 项 | 状态 | 用法 |
|----|------|------|
| Job 落盘续审 | 已做 | `AUDIT_JOB_PERSIST=1`（默认）；`CUTOFF_JOB_ROOT/{job_id}/job_state.json`；重启后 `GET /jobs/{id}` 可恢复 |
| 会计日历 | 已做 | `calendar_mode=natural_month\|fiscal_445\|period_end_only`；`fiscal_year_start`；`PUT /goals` 或环境变量 `AUDIT_CALENDAR_MODE` |
| 截止风险路由（模块27精简） | 已做 | `cutoff_risk_router`；写入截止结果 `风险路由` / `LLM解读.risk_route` |
| 应收同步截止（模块28重叠） | 已有 | `assert_ar_correct_period`；非独立函证 |
| 上游抽样接入（模块30最小） | 已做 | `PUT /jobs/{id}/sample-population`；覆盖图 PARTIAL；链上 `in_sample_population` |
| 真 OCR e2e | 脚本已备 | `ACCEPT_01030_OCR=1 python scripts/accept_gospd01030_ocr_e2e.py`（需 Key，较慢） |
| 抽样设计 / MUS | **不做** | 本系统只接受外部已选样本 |
| 总体完整性勾稽引擎 | **不做** | 仅清单导入提示 |
| 交付包 27/28 全文规格 | **无原文** | 磁盘无交付包目录时无法逐条对原文 |

签收命令：

```text
python -m pytest tests/test_persist_calendar_sample.py tests/test_gospd01030.py tests/test_hitl_gate_formal.py -q
python scripts/accept_gospd01030_gates.py
python scripts/accept_gospd01030_e2e.py
```
