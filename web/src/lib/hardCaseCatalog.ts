/**
 * 识难录：仅保留用户确认的两条演示案例。
 * 未经用户明确允许，禁止追加新条目。
 * 改「多金额 / 拆包」能力时，只更新对应已有条目的字段与日期。
 */

export type HardCaseStatus = 'landed' | 'partial' | 'planned'

export type HardCase = {
  id: string
  title: string
  subtitle: string
  status: HardCaseStatus
  /** 实务里一句话 */
  inPractice: string
  /** 为什么难 */
  whyHard: string[]
  /** 我们的思路（有序） */
  approach: string[]
  /** 目前能做到 */
  canDo: string[]
  /** 刻意边界 / 还不做 */
  boundaries: string[]
  /** 演示时怎么看 */
  howToShow: string[]
  updated: string
}

export const HARD_CASE_CATALOG: HardCase[] = [
  {
    id: 'multi-amount-candidates',
    title: '同页多金额：未税 / 税额 / 价税合计 / 授信串扰',
    subtitle: '发票或结算单上数字很多，系统容易把「授信额度」「行金额」误当成价款字段',
    status: 'landed',
    inPractice:
      '审计抽凭时，一张票上常同时出现行金额、折扣前合计、折后未税、税额、价税合计，甚至授信上限与可用额度。人工一眼能分清角色，OCR/模型若只认「像金额的数字」，就会把含税总额填进不含税字段，或把授信当成应收。',
    whyHard: [
      '难点不在字迹模糊，而在「同一数字形态、多种业务角色」。',
      '英文 key 如 amount 语义过宽，模型容易理解成「最终该付的钱」。',
      '仅靠价税合计一个字段的规则，拦不住「未税被填成价税合计」这类角色错位。',
      '标准增值税模板专精接口对非标模拟票/结算单可能对不上版式，不能当唯一手段。',
    ],
    approach: [
      '规则先挖候选：按标签把数字分成未税、税额、价税合计、折扣前、授信等角色，而不是只抓一个总额。',
      '字段目录定口径：amount = 折后不含税；taxAmount = 税额；totalAmount = 价税合计；并写明排除授信/行金额。',
      '勾稽与冲突触发：未税+税额≠价税合计、未税≈价税合计（角色碰撞）、同角色多候选时，打开「金额待确认」卡。',
      '识别落库后自动跑增值税专精/视觉建议；点进核对页直接看 AI 推荐，不必再等待。',
      '人负责落库：视觉与专精只写建议，采用/手工确认后才写入字段；主流程不自动改数。',
    ],
    canDo: [
      '标准票金额自洽时：不打扰主路径，字段页不弹多余卡片。',
      '疑难票：只开真正有歧义的字段卡（如折后未税 vs 折扣前），不把行数量/税额 300、授信额度塞进候选。',
      '视觉仲裁（已回归）：在多候选图上能区分 amount→折后未税、tax→税额、total→价税合计。',
      '同页多金额：该笔必红、必进原核对页；金额卡默认展开可直接点「采用」，也可手动收起。出口零税（未税=价税、无税额）不误报角色冲突。长 AI 说明默认折叠。',
      'OCR 把千分位逗号认成点（9.683.98）时按千分位还原，不把 9.68 当第二候选；表头「合计金额 / 合计税额 / 价税合计」按下行列对齐，不跨行抓第一个数。',
    ],
    boundaries: [
      '不自动把 AI 推荐写成正式字段（审计责任仍在人）。',
      '非标准增值税版式时，专精接口可能模板不匹配，此时依赖视觉+人工，不伪装成「专精已通过」。',
      '不替代完整 OCR；视觉只做候选排序，不发明新金额。',
    ],
    howToShow: [
      '高级工具 → 识难录 → 打开本条，按「思路 / 做到 / 边界」讲解。',
      '实操：工作台立笔 → 上传含多金额干扰的发票 → 识别完成后该笔红灯 → 点进核对页「金额待确认」默认展开且已有 AI 推荐 → 点采用后，左侧字段与对照表金额变为所选值。',
      '演示快路径按文件名回放首次识别（0296 未税仍是折扣前 68400），并把上次视觉建议一并带回，不现场调视觉。',
      '对照：金额自洽的笔（如仅一张标准票且必填齐）识别后应直接绿灯，不要误报多金额。出口零税订单（如 0285）也不应红灯。',
    ],
    updated: '2026-08-19',
  },
  {
    id: 'mixed-packet-unpack',
    title: '混装扫描件：一包多单、一包多笔',
    subtitle: '客户把合同、订单、发票、签收扫成一个 PDF，不能当成一张单去抽字段',
    status: 'landed',
    inPractice:
      '抽凭常拿到整本扫描件：同一文件里既有一笔业务的全套单据，也有多笔订单混订在一起。若整包当一张单识别，字段会串、对照列会对不上，后面勾稽和测试也会糊成一笔。',
    whyHard: [
      '一个上传文件不再等于一张逻辑单据，也不能默认等于一笔业务。',
      '扫描件往往没有文字层。OCR 常把表头包进 HTML 或 Markdown，规则对不上，后一张单就会被前一张吞掉。',
      '续页常以「二、…」开头，表格单元格里会提到别的单据名，不能当成新表头。有的单连印刷标题都没有，只靠页首主体字段区分。',
      '不能用金额、日期去「猜」哪几页是同一笔，否则会把两笔糊在一起。',
      '确认前不能对整包开字段识别：否则对照和高亮对着未切开的原件，页码也对不上。',
    ],
    approach: [
      '点「开始处理」时拦截混装包：标准件（文件名带订单号/合同号、短 PDF/图片）直接走识别；混装包必须先拆包分笔。',
      '按页切开：先剥 OCR 的 HTML/Markdown；从上往下认第一个表头行（同行取最长标题，公司名写在标题前也能认）。表头变化即切开，续页无新表头并入。',
      '「二、…」续页只扫前两行，避免表格或条款里提到的别的单据名抢类型。合同/订单靠条款表述；发货单看格式字段；发货验收与签收验收看记录主体；发票看表头/票面。票号变化仍切开。',
      '类别卡反证已收紧（对齐 2.0.1）：合同续页写「验收合格后付款 / 销售发货单」不再误伤合同类型。',
      '低质量窗口才打相邻页视觉边界（continue / new_document），并限制每文件调用次数；空白/隔页并入最近单元打标，禁止静默丢页。',
      '用合同号/订单号/发票号把切开的单归到业务笔；没有号的进「未识别业务号」。整包只有一笔时默认一笔，多个互异订单号才拆成多笔。',
      '人工核对：切错就合并或拆开；改单据类型后中间和右边名称马上变；空白/未识别废页可去掉（确认后不进入识别，重新切开还会出现，左边灰色页码可放回）。拖到对应业务笔后确认，才裁成虚拟 PDF。',
      '同一任务拆包分析互斥：页图渲染加锁。React 开发态会连打两次切开，Windows 上并发打开同一 PDF 会渲出空白页，OCR 认不出表头，整包并进第一张回单。',
    ],
    canDo: [
      '一件一文件主路径不变；只有混装扫描 PDF 才出现拆包页。',
      '一笔一包：能按页首表头切出电子回单、合同、订单、发货单、发货验收、签收验收、发票等常见单；确认后对照和高亮对着裁好的单。左边原件预览不再因并发渲染空白而 400。',
      '合同/发货续页不会因正文提到验收、运单、订单号而误切。',
      '多笔混装：多个订单号不会默认糊成一笔；拖到对应业务号后再识别。',
      '拆包页可改类型即时见字；右边业务笔一条条排开、多了就滚，不再叠在一起；疑似空白页虚线标出，可单页或一键去掉，确认后不进识别。',
      '主路径：工作台立笔 → 传凭证 → 混装先拆（未确认不得识别）→ 齐则绿灯。工作台有样本×单据完整性矩阵（✓/?/×）与红黄绿汇总；缺件可复制索要清单。',
      '红灯：明确缺单/缺字段/测试失败（失败行写出左右值，如签收日 vs 入账日）；黄灯：类型存疑。',
      '绿灯笔测试已自动跑完：点进去看结果。测试未通过才进结论页：看对不上的数据和测试逻辑，确认是不通过还是单据问题。',
      '三单/截止规则在工作台可展开查看公式与失败例；导出门禁同步展示红黄绿占比与阻塞项。',
    ],
    boundaries: [
      '同一物理页上多张小票不做二次裁切，整页进一张单。',
      '不按金额、日期静默并笔。禁止静默丢页：没点「去掉这张」的页必须落在某一张单上。',
      '「未识别」类型不进入后面测试。去掉的页确认后不再物化；点「重新切开」还会再出现。',
      '对照表每列只取该类型一张单；同一笔两张发票要在左侧列表点开看。',
      '视觉边界只覆盖低质量窗口，不是整本大 PDF 全页多模态；预算用尽则并入最近单元并标复核。',
    ],
    howToShow: [
      '高级工具 → 识难录 → 打开本条，按「思路 / 做到 / 边界」讲解。',
      '实操：上传多页混装扫描 PDF →「开始处理」或上传页黄条「去拆包分笔」→ 核对切开与归属。规则改过或曾糊成一张单，必须点「重新切开」，刷新页面不会重算。',
      '演示操作：改「单据类型」看中间/右边名称马上变；右边业务笔应能逐条点选、不重叠；虚线页码=疑似空白 →「去掉这页」或「去掉空白页」，不会误删整张合同；左边页码变灰可放回。',
      '标准件（单张发票图、文件名带订单号/合同号的短 PDF）不会进这一步。工作台看矩阵：缺验收应红灯 ×，类型存疑为黄 ?。',
    ],
    updated: '2026-08-19',
  },
]

export function hardCaseStatusLabel(s: HardCaseStatus): string {
  if (s === 'landed') return '已落地'
  if (s === 'partial') return '部分落地'
  return '规划中'
}
